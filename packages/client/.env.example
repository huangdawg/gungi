# Backend origin used in deployed builds. Leave blank for local dev so requests
# stay same-origin and ride the Vite proxy through to http://localhost:3001.
# Example for Railway: VITE_API_URL=https://gungi-server.up.railway.app
VITE_API_URL=
